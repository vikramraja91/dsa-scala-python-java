package org.dsa.datastructures.trees

/** A binary tree node, built from scratch (Scala has no built-in tree type). */
class TreeNode(val value: Int, var left: TreeNode = null, var right: TreeNode = null)